# AGENTS.md

GPT-5.6-Sol이 이 저장소에서 정확도를 유지하면서 작업당 총 토큰을 줄이기 위한 규칙입니다.

## 우선순위와 역할

- 사용자 지시와 상위 안전 정책을 우선합니다. 충돌은 숨기지 말고 한 줄로 알립니다.
- Senior backend/data engineer처럼 성능, 확장성, 정확성을 우선하며 가정을 검증합니다.
- 저장소 사실은 기억이 아니라 코드, GitHub Issue, ADR, 실행 결과로 확인합니다. 미검증 내용은 `추정`으로 표시합니다.
- 문서와 주석은 한국어, 코드 식별자와 Conventional Commit 제목은 영어를 사용합니다.

## 권한과 범위

| 요청 동사 | 허용 범위 |
|---|---|
| 확인·조사·분석·리뷰·설계 | 읽기 전용. 변경하지 않습니다. |
| 수정·구현·적용·고침 | 요청 범위의 최소 변경과 비파괴 검증까지 수행합니다. |
| 커밋·푸시·배포 | 사용자가 명시한 경우에만 수행합니다. |

- 범위가 결과를 바꿀 정도로 모호할 때만 한 번에 질문합니다. 그 외에는 안전한 가정을 밝히고 진행합니다.
- 관련 없는 변경은 수정·포맷·stash·restore하지 않고 스테이징에서도 제외합니다.
- `.env`, Secret, Credential을 수정하거나 커밋하지 않습니다. 파괴 명령, 강제 푸시, 이력 재작성, 운영 배포는 명시적 승인 없이 금지합니다.
- 외부 쓰기나 범위 확대는 확인이 필요하지만, 파일 읽기·범위 내 편집·테스트 같은 로컬 작업은 묻지 않습니다.

## 토큰 효율 목표

대표 작업의 `total_tokens` 중앙값을 기존 `CLAUDE.md` 기준 실행보다 **40% 이상 절감**하면서 동일한 완료 기준과 테스트 결과를 유지합니다. 토큰 절감을 이유로 분석, 안전 검사, 테스트를 생략하지 않습니다.

1. **점진적으로 탐색합니다.** `git status`, `git diff`, `rg`로 후보를 좁힌 뒤 필요한 줄만 읽습니다. 불명확할 때만 파일 전체, 인접 모듈, 저장소 전체 순으로 확장합니다.
2. **이미 얻은 문맥을 재사용합니다.** 같은 파일·Issue·로그를 다시 읽거나 사용자 요청과 규칙을 중간 메시지에서 반복하지 않습니다.
3. **출력을 축소합니다.** 도구 결과는 필터·집계하고 대용량 로그·파일 전문을 대화에 싣지 않습니다. 실패 원문은 원인 판단에 필요한 부분만 보존합니다.
4. **호출을 묶습니다.** 서로 독립적인 읽기 전용 검사는 한 번에 실행합니다. 다음 판단이 필요한 단계나 승인 작업은 분리합니다.
5. **증거가 충분하면 실행합니다.** 코드와 테스트 또는 코드와 ADR이 일치하면 추가 탐색을 멈춥니다. 근거가 충돌할 때만 범위를 넓힙니다.
6. **검증은 위험 기반입니다.** 변경 모듈의 가장 작은 관련 테스트부터 실행하고, 공용 계약·빌드·CI 경계에 영향이 있을 때만 전체 검증으로 확장합니다.
7. **계획과 보고를 압축합니다.** 단순 작업은 즉시 수행합니다. 복수 모듈·고위험 작업만 짧은 계획을 쓰고, 중간 보고는 의사결정·장시간 작업·차단 상황에만 합니다.
8. **병렬 에이전트를 남용하지 않습니다.** 사용자가 요청했거나 독립 작업이 명확하고 벽시계 시간을 크게 줄일 때만 사용합니다. 단일 파일·순차 의존 작업에는 사용하지 않습니다.
9. **최종 답변은 결론 우선입니다.** 근거, 검증 결과, 중대한 주의점, 다음 행동만 남기고 서론·칭찬·작업 과정 재서술을 생략합니다.

토큰 효율 회귀를 측정할 때는 같은 모델·reasoning effort·작업·초기 checkout을 사용해 기존 지침과 이 파일을 각각 실행합니다. 성공 여부와 테스트 통과를 먼저 비교하고, 성공한 실행의 `input_tokens + output_tokens` 중앙값이 기준의 60% 이하인지 확인합니다. 캐시 토큰은 별도로 기록합니다.

## 작업 순서

1. `git status --short --branch`와 원격 차이를 확인합니다. 원격이 앞서고 작업 트리가 안전할 때만 fast-forward 합니다.
2. 관련 Issue의 범위·완료 기준과 관련 ADR을 읽습니다. 확정된 ADR을 바꿔야 하면 코드보다 새 ADR을 먼저 제안합니다.
3. 기존 코드를 분석한 뒤 최소 diff로 구현합니다. 반복되지 않는 로직에 새 추상화를 만들지 않습니다.
4. 범위에 맞는 검사와 테스트를 실행합니다. 실행하지 않은 검사는 통과했다고 말하지 않습니다.
5. 명시적으로 요청된 경우에만 파일별로 스테이징하고 커밋·푸시합니다. `git add .`, `git add -A`는 사용하지 않습니다.

## 프로젝트 기준선

이 프로젝트는 Kubernetes 운영 흐름에 특화된 Observability Dashboard입니다. Grafana 대체용 범용 시각화 도구나 새 수집 파이프라인을 만들지 않습니다.

- `apps/api`: Go Observability API/BFF
- `apps/web`: React/TypeScript UI
- `design-system`: 토큰·컴포넌트·preview
- `packages/contracts`: 공통 계약
- `docs/adr`: 확정된 설계 결정

2026-08-14 기준, UI #14~#17과 Informer #8, Query Catalog #9는 닫혔습니다. GreptimeDB #6, Quickwit #7, OIDC/RBAC #10은 구현 커밋이 있으나 Issue는 열려 있으므로 완료로 추정하지 말고 현재 체크리스트를 확인합니다. Alert UI #17은 닫혔지만 Alertmanager 실제 클라이언트는 남아 있습니다. 주요 후속 작업은 Web-API 실연결, Redis #11, SSE #12, 배포·운영·통합 E2E #19~#22입니다.

세부 규칙은 중복 적재하지 않고 작업 범위에 따라 다음 원문만 읽습니다.

- 전체 구조·비기능 요구: `README.md`
- 프런트엔드: `apps/web/README.md`
- 백엔드: `apps/api/README.md`
- 디자인: `design-system/README.md`
- 설계 결정: `docs/adr/`

## 변경 불가 원칙

### 보안·데이터 경계

- UI 입력의 Cluster/Namespace를 신뢰하지 않고 서버가 권한 Scope를 강제 삽입합니다.
- 브라우저는 데이터소스를 직접 호출하거나 Raw PromQL/SQL/Quickwit Query를 보내지 않습니다. 등록된 `queryRef`와 BFF만 사용합니다.
- 캐시 키와 SSE에 사용자 Scope를 포함합니다. 로그 마스킹은 원문을 내리기 전에 서버에서 수행합니다.
- 요청 경로에서 Kubernetes API를 호출하지 않고 informer cache를 읽습니다. 폴링을 추가하지 않습니다.

### 성능·정확성

- 화면 단위 집계 endpoint를 사용하고 위젯별 N+1 요청을 만들지 않습니다.
- 부분 장애는 `Section<T>` 값으로 표현하며 데이터 없음, 권한 없음, upstream 장애를 구분합니다.
- 시계열은 범위별 step/downsampling과 최대 포인트를 서버에서 강제합니다.
- 로그는 cursor/search-after를 사용하며 offset deep pagination을 금지합니다.
- 취소를 upstream까지 전파하고 timeout·circuit breaker를 유지합니다.
- Pod/Workload는 이름보다 UID로 식별하고 큰 목록은 가상화합니다.

### UI·디자인

- 원시 hex·임의 spacing 대신 `design-system/tokens`의 역할 토큰을 사용하고 Light/Dark를 모두 확인합니다.
- 상태는 색만으로 전달하지 않습니다. 차트는 이중 y축과 계열 색 순환을 금지합니다.
- 알림 화면은 조회 전용이며 Rule·Silence·Routing 편집 기능을 만들지 않습니다.

## 검증 명령

변경 범위에 필요한 명령만 실행합니다.

- 공통: `git diff --check`
- Web/Design: `make check`, `make build`, `make test-web`
- API: `make api-vet`, `make api-test`, `make api-build`
- informer 리소스·protobuf 협상·실데이터 어댑터: `make api-itest` (환경 미제공 시 skip을 명시)

## 완료 보고

구현 작업의 최종 답변은 다음 다섯 항목만 간결하게 보고합니다.

1. 변경 파일과 핵심 수정
2. 실행한 명령
3. 테스트 결과와 실패·skip
4. 성능 영향
5. 남은 위험
