# 기여 가이드

이 문서는 확정된 아키텍처 결정을 바꾸는 기술 변경 절차만 다룹니다. 일반 개발·코딩 규칙은
각 영역의 README와 저장소 지침을 따릅니다.

## ADR 변경 절차

`docs/adr/`의 **Accepted** ADR에서 결정 내용은 직접 고치지 않습니다. 오탈자·링크·상태 같은
메타데이터는 사실에 맞게 갱신할 수 있습니다. 결정을 변경하거나 예외를 추가해야 하는 작업은 코드보다
먼저 다음 번호의 새 ADR을 **Proposed** 상태로 추가합니다.

1. 새 ADR에 배경, 결정, 검토·기각 대안, 결과와 위험, 후속 작업을 기록합니다.
2. 대체할 ADR이 있으면 관련 항목에 번호를 명시하되 기존 원문은 유지합니다.
3. 리뷰에서 보안·데이터 경계, 성능·운영 영향, 마이그레이션과 롤백 가능성을 확인합니다.
4. 승인 후 새 ADR을 **Accepted**로 바꾸고, 대체된 ADR은 **Superseded by NNNN**으로 표시합니다.
5. 그 다음 코드 변경을 작성하거나 병합합니다. 미채택 Proposed ADR은 병합하지 않으며, 논의 기록을
   보존해야 한다면 상태와 보존 방식을 별도로 합의합니다.

상태 값은 `Proposed` → `Accepted`, 기존 결정은 새 결정 승인 후
`Accepted` → `Superseded by NNNN`으로 전이합니다. `Deprecated`는 유효했던 결정을 대체 결정 없이
더 이상 사용하지 않을 때만 적용합니다.

## PR 체크

- [ ] Accepted ADR의 결정 내용을 직접 변경하지 않고 새 Proposed ADR을 먼저 추가했습니다.
- [ ] 변경 대상 ADR과 관련 Issue를 연결했습니다.
- [ ] 보안 경계, 성능·운영 영향, 대안과 위험, 마이그레이션·롤백을 검토했습니다.
- [ ] ADR 승인 전에는 결정에 의존하는 코드를 병합하지 않습니다.
- [ ] 승인 시 ADR 인덱스와 대체 상태를 함께 갱신합니다.
